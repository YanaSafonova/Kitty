#include <iostream>
#include <string>
#include <fstream>
const int size=
using namespace std;
struct sms
{ string type;
String txt;
};
struct spam
{string name;
int index;
};
void create(ifstream &input,sms *a,size)
{a=new sms[size];
for (int i=0;i<size;i++)
input>>a[i].type<<a[i].txt;
}
